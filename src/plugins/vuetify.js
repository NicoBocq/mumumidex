import Vue from "vue";

import {
    Vuetify,
    VApp,
    VCard,
    VBottomSheet,
    VAppBar
} from "vuetify";

Vue.use(Vuetify, {
    components: {
        VApp,
        VCard,
        VBottomSheet,
        VAppBar
       /* other imports */
    },
    /* theme option */
});